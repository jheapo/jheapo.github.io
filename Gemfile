source "https://rubygems.org"

# Local development: `bundle exec jekyll serve`.
# The site uses no theme gem, so plain Jekyll is enough.
gem "jekyll", "~> 4.3"

# Uncomment to build exactly as GitHub Pages does instead of plain Jekyll:
# gem "github-pages", group: :jekyll_plugins
